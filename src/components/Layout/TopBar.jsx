import { Box, HStack, Text } from "native-base";
import React from "react";

export const TopBar = ({ navigation }) => {
  return (
    <HStack>
      <Box>
        <Tex>Back</Tex>
      </Box>
    </HStack>
  );
};
